services:
  backend:
    build: .
    ports:
      - "8080:8080"
      - "9090:9090"
    env_file: .env
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/ranking.db
    restart: unless-stopped
