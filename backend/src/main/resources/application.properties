spring.datasource.url=jdbc:sqlite:${DB_PATH:ranking.db}
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=

# Ranking API Token (Prod: echten Token als Env-Var setzen)
RANKING_TOKEN=${RANKING_TOKEN:dev}

# TCP Game Server Port
game.server.port=${GAME_PORT:9090}
